# Урок 1: Створення HTML-сторінки для малювання на Canvas

## Крок 1: Відкрити ChatGPT

Перейдіть за посиланням на ChatGPT та відкрийте новий чат.

Скопіюйте та вставте у чат наступний промпт:

**Промпт:**

> "Створи HTML-сторінку з 2D canvas, де можна малювати кольорові квадрати кліком миші, зі стартовим розміром квадрата 10px і можливістю вибору кольору."

Попросіть ChatGPT згенерувати подібну сторінку або використайте готовий приклад, що наведений нижче:

```html
<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>2D Канвас для малювання</title>
    <style>
        #drawingCanvas {
            border: 1px solid #000;
            cursor: crosshair;
            display: block;
        }
        .controls {
            margin-bottom: 10px;
        }
        .controls label {
            margin-right: 10px;
        }
    </style>
</head>
<body>

    <h1 style="background: white;">Малювання на Канвасі</h1>

    <div class="controls" style="background:white ;">
        <label for="colorPicker">Виберіть колір:</label>
        <input type="color" id="colorPicker" value="#007bff">
    </div>

    <canvas id="drawingCanvas" width="500" height="500" style="background-color: white;">
        На жаль, ваш браузер не підтримує елемент canvas.
    </canvas>

    <script>
        const canvas = document.getElementById('drawingCanvas');
        const colorPicker = document.getElementById('colorPicker');
        const ctx = canvas.getContext('2d');
        const squareSize = 10;

        canvas.addEventListener('click', (event) => {
            const selectedColor = colorPicker.value;
            const rect = canvas.getBoundingClientRect();
            const clickX = event.clientX - rect.left;
            const clickY = event.clientY - rect.top;

            const startX = 10*Math.floor(clickX / 10);
            const startY = 10*Math.floor(clickY / 10);

            ctx.fillStyle = selectedColor;
            ctx.fillRect(startX, startY, squareSize, squareSize);
        });
    </script>

</body>
</html>
```

---

## Крок 2: Створити текстовий файл на вашому комп’ютері

### Якщо ви користуєтесь **Windows**:

1. Натисніть **ПКМ → Створити → Текстовий документ**
2. Назвіть файл: **pixelart.html**
3. Відкрийте файл подвійним кліком і вставте HTML-код
4. Збережіть (**Ctrl + S**)

### Якщо ви користуєтесь **MacBook**:

1. Відкрийте **TextEdit**
2. Натисніть **⌘ + N** (новий документ)
3. У меню виберіть: **Format → Make Plain Text** (Зробити простим текстом)
4. Вставте HTML-код і збережіть файл як **pixelart.html**

---

## Крок 3: Перегляд результату

1. Знайдіть файл **pixelart.html** на комп’ютері
2. Двічі натисніть на нього

Він автоматично відкриється у браузері і ви зможете:
✨ Малювати квадрати кліками миші
🎨 Обирати колір у палітрі

---

> Вітаємо! Ви створили свою першу інтерактивну веб-сторінку! 🚀
